{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Package",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceRoot}/cmd/api",
      "envFile": "${workspaceRoot}/.env",
      "cwd": "${workspaceRoot}",
      "args": [],
      "buildFlags": "-gcflags='all=-N -l'"
    }
  ]
}
